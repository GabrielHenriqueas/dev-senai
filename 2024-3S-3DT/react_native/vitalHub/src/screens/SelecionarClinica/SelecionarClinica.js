import { ScrollView } from "react-native"
import { ContainerCard } from "../../components/CardSelecionarClinica/StyledCardSelecionarClinica"
import { Container } from "../../components/Container/StyledContainer"

export const SelecionarClinica = () => {
    return (
        <Container>
            <ScrollView>
                <ContainerCard>

                </ContainerCard>
            </ScrollView>
        </Container>
    )
}